#use open jdk
box:
id: openjdk
ports:
- $PORT

#build using mav
build:
  steps:
    - install-packages:
    packages: maven
    - script:
       name: maven build
       code: mvn clean assembly:assembly
